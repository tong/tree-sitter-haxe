=============
empty package
=============

package;

---

(module (package_stmt))

=============
package parts
=============

package my.other.package;

---

(module
  (package_stmt
    (package_path
      (pack)
      (pack)
      (pack))))
